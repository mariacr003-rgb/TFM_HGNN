# Fase 1 - Pipeline de datos (Python del sistema)
# Solo usa modulos estandar de Python: csv, sys, pathlib
# matplotlib se usa para las figuras 
matplotlib==3.10.9

# Fase 2 - Framework GAT+GCN+VGAE (entorno virtual venv_pytorch, Python 3.11)
torch==2.13.0
torch_geometric==2.8.0

# Bloque 8 - MIL sobre WSI (mismo entorno venv_pytorch)
torchvision==0.28.0
openslide-python==1.4.6
openslide-bin==4.0.1.2

# Baseline SVM/RF (Seccion 1.3 del TFM) - entorno virtual NUEVO Y
# AISLADO (tfm_entorno/venv_baseline_svm_rf, Python 3.11), NO
# venv_pytorch: no necesita torch_geometric para nada, y mantenerlo
# aparte evita cualquier riesgo de conflicto de version con el
# entorno de GAT+GCN/VGAE/MIL/DEC ya validado. torch se instala aqui
# tambien, pero solo se usa para leer los .pt del proyecto (I/O), no
# para ningun calculo.
torch==2.13.0
scikit-survival==0.28.0
scikit-learn==1.9.0