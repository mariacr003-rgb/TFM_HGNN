# Fase 1 - Pipeline de datos (Python del sistema)
# Solo usa modulos estandar de Python: csv, sys, pathlib
# matplotlib se usa para las figuras 
matplotlib==3.10.9

# Fase 2 - Framework GAT+GCN+VGAE (entorno virtual venv_pytorch, Python 3.11)
torch==2.13.0
torch_geometric==2.8.0

# Bloque 8 - MIL sobre WSI (mismo entorno venv_pytorch)
torchvision==0.28.0
openslide-python==1.4.6
openslide-bin==4.0.1.2

# Baseline SVM/RF (Seccion 1.3 del TFM): NO instalar en este entorno
# (venv_pytorch_wsl) - requiere un venv NUEVO Y SEPARADO
# (tfm_entorno/venv_baseline_svm_rf, Python 3.11), con sus propias
# dependencias fijadas en entorno/requirements-baseline-svm-rf.txt
# (torch, scikit-survival, scikit-learn). Motivo: scikit-survival fija
# rangos de version de numpy/scipy/scikit-learn que podrian entrar en
# conflicto con los ya fijados por torch/torch_geometric en este
# entorno, ya validado en 4 bloques (GAT+GCN, VGAE, MIL, DEC) - aislar
# el entorno elimina ese riesgo de raiz en vez de solo mitigarlo. Ver
# entorno/requirements-baseline-svm-rf.txt y
# results/2026-08-20-paso55/runlog.txt para el detalle completo.